@model SpeciesAssessment2021
@*
    @model IEnumerable<Artsdatabanken.RL2021Reference>
    @{
        // Copied unfinished code from innsynet2021. Leave in case relevant later.
        //var publications = Model.Where(x => x.Type == "Publication");
        //var urls = Model.Where(x => x.Type == "Url");
        //var persons = Model.Where(x => x.Type == "Person");
        //var other = Model.Where(x => x.Type != "Publication" && x.Type != "Url" && x.Type != "Person");
    }
*@


@if (@Model.References != null && Model.References.Count() > 0)
{
<div class="page_section">
    <h2>
        Referanser
    </h2>
    @if (Model.References.Count() > 0)
    {
        <ul>
            @foreach (var pub in Model.References)
            {
                <li class="reference">@pub.FormattedReference</li>
            }
        </ul>
    }
</div>
}
    